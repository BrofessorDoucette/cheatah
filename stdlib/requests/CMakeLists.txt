# requests — THE FIRST PURE-CHEATAH STDLIB MODULE: HTTP authored in .purr, compiled by purrc
# into a transparent importable module (generated C++ inlined in the committed header).
cheatah_add_module(requests SOURCES requests.purr TRANSPARENT)
