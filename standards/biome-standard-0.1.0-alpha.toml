# Biome Standard 0.1.0-alpha — the set of component releases tested to work together.
# Append-only: once released, only the status line may change (security advisories).

[standard]
version = "0.1.0-alpha"
released = "2026-07-25"
status = "current"

[components]
cheatah = "v1.7.0-alpha"
cheatah-gpu = "v0.5.0-alpha"
